# Ignore the node modules folder (created by 'npm install').
node_modules

# We absolutely don't want to have the .sass-cache in git.
.sass-cache

# Ignore any errors thrown by npm.
npm-debug.log

# Ignore dist directory of compiled assets.
dist
